apiVersion: 1

providers:
  - name: Canary Maritime Dashboards
    orgId: 1
    folder: Canary Maritime
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
